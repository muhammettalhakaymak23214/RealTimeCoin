import 'package:flutter/material.dart';

class AppColors {

  AppColors._();

  // UI Colors
  static const Color primary = Color.fromRGBO(247, 147, 26, 1); 
  static const Color secondary = Color.fromARGB(255, 36, 22, 54);
  
  // State Colors
  static const Color success = Colors.green;
  static const Color error = Colors.red;
  
  // Background and Card Colors
  static const Color scaffoldBg = Color.fromARGB(255, 14, 14, 17);
  static const Color cardBg = Color.fromRGBO(247, 147, 26, 1); 
  
  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}